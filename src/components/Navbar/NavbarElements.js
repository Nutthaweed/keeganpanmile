import styled from 'styled-components'

export const Nav = styled.nav`
    background: black;
    height: 10px;
    display: flex;
    justify-content: space-between;
    padding: 2.5rem;
    z-index: 10;
    color: #fff;
`